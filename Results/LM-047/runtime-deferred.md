# LM-047 runtime evidence deferred to H9

LM-047 is implementation-ready but technically blocked. Safe local evidence proves the
atomic provisional-deletion transaction, exact Moment and half-open Range identity,
immediate suppression across database query surfaces, cancellation of pending derived work,
durable rewrite queue, content-free audit, failure projection, encrypted-database restart,
late-search read barrier, Cancel-default model, deterministic UI copy, and native arm64
compile.

H9 must still run the real application flow on the isolated validation Mac: keyboard and
VoiceOver confirmation with Cancel as the default, Moment and Range selection, immediate
disappearance from Search/Timeline/helper clients while a real rewrite is queued, visible
progress/failure/retry behavior, quit/relaunch recovery during processing, and final
verified physical deletion. No local unit/model/static/compile/snapshot evidence is a
substitute for those checks.

No application, ScreenCaptureKit, Apple ImageIO, VideoToolbox, hardware-media runtime, or
software HEIC helper runtime was executed on this laptop for LM-047.
