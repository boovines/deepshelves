# LM-060 runtime evidence deferred to H9

The safe implementation copies retained independent frame assets byte-for-byte, verifies their
recorded byte counts and SHA-256 identities, rebuilds the canonical manifest, atomically swaps
database references, compacts the affected vector model, removes every deleted derivative, and
recovers from six injected crash boundaries. Synthetic fixture bytes deliberately are not HEIC
and therefore make no decode claim.

LM-060 remains technically blocked because its acceptance criterion requires the retained
assets to remain decodable in the production codec path and requires the real application
Moment/Range/relaunch journey. Both are prohibited on this laptop. H9 must decode every retained
asset with the pinned production software HEIC decoder, inspect the live search/timeline/detail
result, exercise the UI deletion flow and restart recovery, and confirm no unexpected media
service appears. Unit integrity, static checks, and compile-only evidence are not substitutes.

No application, ScreenCaptureKit, Apple ImageIO, VideoToolbox, software HEIC helper, or hardware
media runtime executed during this checkpoint.
