# LM-045 runtime evidence deferred to H9

LM-045 remains technically blocked. Its timeline source, model, cache, and SwiftUI composition
are ready for downstream safe work, but its original acceptance requires application and media
runtime evidence that is prohibited on this laptop.

On the physically distinct isolated validation Mac, run the exact committed revision under the
encoder-service tripwire and deny-all network policy:

1. Use the deterministic 24-hour S6 timeline corpus and production thumbnail/detail paths.
   Record at least 100 representative scrub samples and prove thumbnail feedback below 50 ms
   p95 and full-frame settlement below 200 ms p95, including cold and cached paths.
2. Perform at least 100 rapid nonadjacent scrubs with delayed thumbnail and full-frame work.
   Inspect the rendered pixels and provenance after every completion; no stale frame, locator,
   epoch, target window, or thumbnail may appear.
3. Exercise pointer scrubbing, left/right frame movement, all four zoom levels, every canonical
   gap class, and the chronological accessibility list. Preserve a keyboard transcript and a
   VoiceOver transcript proving transition, gap, selection, boundary, value, and action output.
4. At default and minimum window sizes, capture light, dark, and Increased Contrast baselines.
   Repeat the critical controls with English pseudo-localized to 140% length and verify no
   clipping, hidden controls, illegible patterns, missing focus rings, or color-only meaning.
5. After a cached success, separately change current suppression state, locator metadata, file
   type/symlink state, bytes, and SHA-256 identity. Confirm no cached thumbnail or full frame is
   returned after current-source rejection.
6. Preserve raw timing samples, content-free interaction/accessibility transcripts, screenshot
   baselines, exact revision, host declaration, and tripwire output in the H9 ledger.

Unit, model, static, compile-only, and deterministic snapshot evidence in this directory is
supporting implementation evidence only. It does not satisfy these runtime checks.
