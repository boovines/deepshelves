# LM-074 runtime evidence deferred to H9

No application, XCUITest, ScreenCaptureKit, Apple ImageIO, VideoToolbox, or hardware-media
runtime was executed on this laptop. Unit/model/static/snapshot evidence proves exact totals,
copy constraints, source composition, and compile closure only; it does not prove the
installed interaction or rendered layout.

H9 must verify the signed installed application at default and minimum window sizes for:

- day/week and date controls updating the selected elapsed interval;
- every per-application duration plus `Unrecorded` reconciling to that interval;
- keyboard and VoiceOver access to totals and range controls; and
- neutral copy and layout in light, dark, and Increased Contrast appearances.

Any mismatch leaves LM-074 blocked.
