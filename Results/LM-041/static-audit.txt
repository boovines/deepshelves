LM-041 static audit — 2026-08-29

PASS: the production result surface uses NSCollectionView reuse and creates/configures only
requested collection items; it no longer creates every result through a SwiftUI List.
PASS: the adaptive layout is one/two/three columns below 520/from 520 through 719/at least
720 points, and the same pure projection is covered by model tests.
PASS: pagination forwards the engine's opaque cursor unchanged, rejects a late query
generation, appends in page order, removes duplicate frame IDs, and preserves the current
page on an error with a stable diagnostic code.
PASS: one root-owned actor cache is injected through SearchSessionModel. Its exact identity
contains frame ID, locator kind, and locator value; per-frame generations reject a stale
completion and a byte-bounded LRU bounds decoded raster memory.
PASS: production thumbnail lookup requires an exact ready metadata locator, regular
non-symlink file, matching SHA-256, and pinned software-codec decode before exposing pixels.
PASS: cards project capture time, application, approved host, evidence source, matched text,
position, and total count. Empty archive/query, loading, no-result/removable-filter,
indexing, pagination-error, and fatal-error states are explicit.
PASS: result changes create deterministic accessibility announcement text and cards expose
stable surface-specific identifiers, labels, button roles, and Return activation.
PASS: 19 MemorySearch unit/model tests pass, including exact cursor paging, stale thumbnail
rejection, explicit state/card projection, and a deterministic 10,000-card projection.
PASS: strict swift-format lint and universal Release compile-only verification pass.
PASS: privacy and dependency static gates pass.
PASS: process prechecks and the 50 ms encoder-service tripwire remained clear.

NOT EXECUTED: application launch, UI tests, NSCollectionView runtime rendering, S6 timing or
memory measurement, keyboard interaction, VoiceOver announcements, ScreenCaptureKit,
Apple ImageIO, VideoToolbox, or hardware-media runtime. The model's 10,000-card correctness
check and compile-only proof are not substitutes for the deferred runtime evidence.
