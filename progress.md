# DeepShelves build progress

This file is append-only. Each story records measurements, failures, and durable implementation lessons needed by a replacement goal thread.

## 2026-08-27 — LM-001 started

- Preflight: Apple Silicon (`arm64`), macOS 26.5, Xcode 26.5 (17F42), 163 GiB free.
- GitHub CLI is authenticated as `boovines`; `boovines/deepshelves` was created with private visibility and configured as `origin`.
- XcodeGen 2.46.0 was installed as the pinned bootstrap generator. All LM-001 Swift package dependencies are local, so project generation and compilation require no dependency network access.
- The source planning collection was copied byte-for-byte into `Docs/Plan/`; that committed snapshot becomes canonical at the LM-001 checkpoint.

## 2026-08-27 — LM-001 passed

- The first Xcode project probe found an incomplete first-launch installation (`DVTDownloads.framework` missing). `xcodebuild -runFirstLaunch` repaired the installation automatically; no H0 human gate was needed.
- XcodeGen emits a disposable `LocalMemory.xcodeproj`; the committed `LocalMemory.xcworkspace` points to it. Generated project data and DerivedData remain ignored.
- Tests initially failed because an integration bundle inherited a test host path based on the target name while the app product has a human-readable name. Removing the unnecessary app-host dependency made the integration suite logic-only and stable.
- Debug unit, integration, privacy, and performance tests pass. The release app, CLI, and MCP targets compile with Swift 6 strict concurrency and warnings as errors.
- A fresh no-local clone regenerated the project, passed privacy smoke and all tests, and completed the release build with automatic package resolution disabled. All packages in this checkpoint are local.
- Stable checkpoint revision: `refs/tags/checkpoints/LM-001`. Evidence: `Results/LM-001/build.txt`.

## 2026-08-27 — LM-002 blocked at H1

- Fixed bundle IDs now use `com.justinhou.deepshelves.localmemory` with `.cli` and `.mcp` helper suffixes.
- All three products are configured for Hardened Runtime, App Sandbox off, Swift 6, macOS 15 minimum, and the same `$(AppIdentifierPrefix)com.justinhou.deepshelves.shared` Keychain access group. No network entitlement is present.
- `ArchivePathProvider` creates the canonical `~/Library/Application Support/LocalMemory/` tree with directory mode 0700 and publishes file mode 0600 for owning writers. Deterministic temporary-root tests pass.
- Privacy smoke, affected tests, and the unsigned release build pass. The signed-posture proof is intentionally fail-closed while `security find-identity -v -p codesigning` reports zero valid identities.
- Human gate H1 is required. Resume from `HUMAN-ACTION.md` and `Results/LM-002/H1-preflight.txt`.

## 2026-08-27 — LM-002 H1 resume attempt

- The user believed signing setup might be complete, but the authoritative identity probe still returned zero valid identities. Keychain contained no Apple Development certificate, Xcode's active production-account list was empty, and no provisioning profiles existed.
- Apple’s current Xcode help confirms the sequence: add the Apple Account in Accounts settings, select the team, open Manage Certificates, click the lower-left Add button, and choose Apple Development. `HUMAN-ACTION.md` now records this exact flow.
- A read-only computer-use inspection was abandoned because the ChatGPT Computer Use permission prompt remained incomplete; it is not part of H1 and no additional human gate was created.

## 2026-08-27 — LM-002 H1 resolved

- The user created an Apple Development certificate for Personal Team `NS5L7NNR8U`, but `security find-identity` initially still returned zero because the login/system keychains only contained Apple’s expired 2023 WWDR intermediate.
- The leaf certificate’s issuer is WWDR G3. Apple’s official G3 certificate was downloaded from `https://www.apple.com/certificateauthority/AppleWWDRCAG3.cer`, verified as SHA-256 `dcf21878c77f4198e4b4614f03d696d89c66c66008d4244e1b99161aac91601f`, and added to the login keychain. This repaired the trust chain without importing a private credential.
- `security find-identity -v -p codesigning` now reports one valid Apple Development identity. The project is fixed to team `NS5L7NNR8U`; signed-product verification resumed.

## 2026-08-27 — LM-002 H1 Keychain authorization pause

- The signed scheme initially launched app, CLI, and MCP `codesign` operations concurrently, causing three identical Keychain dialogs to stack. The build was interrupted and the verifier now signs three explicit schemes sequentially.
- The user reported that entering a password did not dismiss the frontmost prompt. No signing process remains, so the visible prompts are orphaned and safe to dismiss. This is distinct from the now-valid certificate trust chain.
- The prompt requires the `login` keychain password—normally the current Mac login password, but potentially an older Mac password if the login keychain became out of sync. Do not use the Apple Account password and do not reset/delete the keychain.
- A clean sequential rerun is pending direct login-keychain unlock verification under H1.

## 2026-08-27 — LM-002 passed

- H1 was completed by unlocking the existing `login` keychain; no reset or credential migration was required. The sequential verifier signed the app, CLI, and MCP helper with Apple Development identity `5F2CF76A05528002A43A04B748E50F81D795A4E9`.
- All three signed products verify strictly, carry Hardened Runtime, share `NS5L7NNR8U.com.justinhou.deepshelves.shared`, and carry neither App Sandbox nor client/server network entitlements. The project fixes team `NS5L7NNR8U`, its three bundle IDs, and macOS 15.0 deployment.
- `ArchivePathProviderTests` prove the archive tree is mode `0700`, published files are mode `0600`, and caller-supplied root names cannot escape Application Support.
- Full Xcode tests, privacy smoke, and unsigned Release build passed. Test and release scripts must not run concurrently because both call XcodeGen on the same `.xcodeproj`; a parallel gate attempt produced a harmless destination-exists race, and the release gate passed when rerun sequentially.
- Acceptance evidence is recorded in `Results/LM-002/entitlements.txt`.

## 2026-08-27 — LM-003 passed

- `MemoryContracts` now owns the V1 Foundation-only vocabulary for capture, media, searchable frames, text, enrichment, bounded search, stable result pages, timeline gaps/transitions, agent policies, deletion tombstones, and recoverable processing jobs. Runtime pixel buffers are explicitly omitted from serialization.
- `ContractJSON` is the canonical boundary codec. It emits sorted canonical JSON, lowercase UUID strings, and RFC 3339 UTC timestamps with exactly millisecond precision; unknown additive fields are ignored while unknown enums and noncanonical timestamps fail closed.
- Validation fixes normalized coordinates to upper-left-origin `0...1`, media intervals to half-open and at most 30 seconds, archive locators to relative non-traversing paths, agent history to 30 days, agent sessions to 24 hours, result bounds to 100, and automatic job attempts to three. Empty agent allowlists mean no content.
- Search pages sort by fused score descending, capture time descending, then lowercase UUID lexical order. Timeline members sort chronologically and excluded/protected/policy-uncertain gaps reject application identity.
- Eleven synthetic CC0 V1 fixtures are manifest-pinned by SHA-256. `scripts/check-contracts.sh` rebuilds the fixture generator, reproduces every byte, verifies every manifest hash, and runs 21 focused behavior/compatibility tests.
- The full repository suite passes 27 tests with zero failures; privacy smoke and unsigned offline Release build pass. Evidence: `Results/LM-003/contracts.json`.

## 2026-08-27 — LM-004 passed

- The canonical dependency/model SBOM now pins 21 components by exact version and commit, records license text hashes, artifact sizes/SHA-256 values, linkage scope, runtime-fetch policy, and an update procedure. Forty-one source, binary, and model artifacts total 402,123,412 bytes.
- The SQLCipher-maintained GRDB 7.11.1 fork is paired with SQLCipher.swift 4.18.0 and its package-declared XCFramework digest. This is the current supported SwiftPM shape for the fixed GRDB/SQLCipher architecture; database integration remains owned by LM-017/S5.
- Official MCP Swift SDK 0.12.1 currently declares EventSource and HTTP/conformance dependencies. The upstream package, EventSource, and swift-nio are recorded but forbidden from shipping. LM-068 must form an audited official-source stdio-only local target and prove the release helper links none of them.
- Argmax OSS 1.1.0 contains model-download configuration and an optional server build. It remains forbidden from shipping until optional audio creates a local-model-only target with `BUILD_ALL` disabled. The 19-file `small.en` 217 MB variant is pinned but remains off and build-time-install-only.
- Apple's six-file MobileCLIP-S0 Core ML pair and the official tokenizer/reference source are byte-pinned. The model license is research-only and excludes commercial product use; the private research build may evaluate it, but redistribution or commercial release requires a different grant or approved model decision.
- XcodeGen 2.46.0, Core ML SDK, Swift Testing, XCTest, and XCUIAutomation are tied to Xcode 26.5 build 17F42. Apple-licensed Xcode is installed separately rather than placed in the dependency cache.
- A fresh cache fetched and verified all 41 artifacts. The offline verifier then passed with `curl` exported as a failing function, proving that `--offline` issued no network call. Full tests increased from 27 to 33 and pass; contract fixtures, privacy smoke, Release build, and linked-binary scans of app/CLI/MCP all pass.
- Stable checkpoint: `refs/tags/checkpoints/LM-004`. Evidence: `Results/LM-004/dependencies.json`.

## 2026-08-27 — LM-005 passed

- The signed Release S1 harness uses one long-lived single-window `SCStream`, public-API AX-to-`SCWindow` resolution, revocable capture epochs, a final focus/policy/window/dimension/epoch admission check, and metadata-only typed gaps for uncertainty. H2 was verified against the final binary: Screen Recording and Accessibility are both granted.
- An early one-hour run showed that wall-clock rollover at 30 seconds can yield approximately 30.56-second QuickTime duration because the final ScreenCaptureKit sample interval extends the asset. Production rollover is therefore 29 seconds while the contract remains at most 30 seconds; boundary crash points 28/29/30 recovered at 28.045/29.053/29.560 seconds.
- A later rehearsal stopped advancing after about nine minutes because an asynchronous `SCShareableContent` refresh never completed. The production refresh provider now has a two-second fail-closed timeout, 30-second duplicate-suppression/replacement lease, and token ownership so a late completion cannot clear a newer lease. A 15-minute rehearsal and the final hour both advanced continuously beyond the former failure point.
- Final active results: 2,458/2,458 eligible transitions correct within one second, 352 excluded/unresolved transitions, 2,341 stale/policy frames rejected, zero contamination, zero backpressure drops, zero writer errors, and 351 sleep/wake cycles. Mean/p95 CPU was 3.948%/8.5%, max RSS 97.094 MB, storage 43.808 MB/hour, and random decode p95/p99 12.615/16.654 ms.
- Final static results: mean/p95 CPU 0.773%/1.1%, max RSS 85.188 MB, storage 0.519 MB/hour, ten 30-second heartbeat frames, zero contamination/backpressure/writer errors.
- All 2,419 referenced active/static assets passed HEVC and duration validation. Thirty forced-termination points produced playable HEVC artifacts, with maximum recovered duration 29.56 seconds. A separate 30-second Time Profiler trace was recorded without perturbing the canonical resource samples.
- The production defaults are frozen by 13 unit tests and one hardware-HEVC integration test. The full 47-test suite, 11 contract fixtures, privacy smoke, and universal offline-capable Release build pass. No 1,680-pixel, 0.5 fps, or HEIC fallback was used. Evidence: `Results/LM-005/report.json`, `Decisions/0001-capture-media.md`, and `Benchmarks/Results/S1/20260827T173516Z/`.

## 2026-08-27 — LM-006 passed

- The S2 harness projects only bounded, public Accessibility attributes, suppresses secure-field values, associates observations with the resolved window, and enforces both a 45 ms caller cutoff and the AX messaging timeout. Its live validation inspects only DeepShelves' synthetic window; it does not read or persist personal content from installed applications.
- The canonical 250-fixture AX corpus was 100% useful with 0.025 ms traversal p95. All 25 signed live probes were useful with 11.681 ms p95 and no timeout or capture-thread blocking.
- Apple Vision recovered all 800 words at IoU at least 0.5 across 200 locally rendered fixtures, including all 150 high-contrast fixtures. The stable AX-first merge left zero duplicate normalized tokens and lost zero ground-truth tokens.
- All 500 Safari/Chromium/Firefox structural adapter fixtures detected the correct sanitized host. Private mode suppressed 60/60 cases, and URL-unavailable site-rule cases suppressed 60/60 within the same decision frame. URL normalization removes credentials, query, and fragment components.
- The exclusion/private/background corpus evaluated 1,920 media, thumbnail, text, title, URL, vector, cache, and log slots with zero persisted prohibited artifacts. Named app coverage is represented honestly as installed inventory plus deterministic structural fixtures; Edge was absent and used fixture validation only.
- The full 54-test suite, 11 contract fixtures, privacy smoke, universal offline Release build, release binary audit, and signed S2 evidence checker pass. No fallback was needed by the canonical corpus. Evidence: `Results/LM-006/report.json` and `Benchmarks/Results/S2/20260827T191035Z/`.

## 2026-08-27 — LM-007 passed

- The selected MobileCLIP-S0 image and text Core ML encoders, tokenizer vocabulary/merges, and integrity manifest are compiled into `MemoryEnrichment`. Startup streams and verifies all 12 artifact hashes before model loading; the signed Release benchmark ran under an explicit `deny network*` sandbox and permits no runtime fetch.
- Packaged/reference cosine parity measured 1.0 for image and 0.99999988 for text. Across 500 locally rendered frozen frames and 100 frozen queries, real Core ML image/text p95 latency was 1.546/7.919 ms, visual Recall@10 was 1.0, and the single-job actor produced zero capture-timer intervals over 100 ms. The packaged resources occupy 112,239,644 bytes and enrichment max RSS was 351.609 MB.
- Mapping the entire 1,024,000,128-byte Float16 vector file made the first implementation fast but raised RSS to roughly 1.7 GB. The accepted scanner page-aligns bounded read-only `mmap` chunks, converts Float16 with vImage, scores with Accelerate, applies `MADV_DONTNEED`, and unmaps immediately. At one million 512-dimensional vectors it measured 618.043 ms p95 and 637.555 ms p99 unfiltered, 64.117 ms p95 over 100,000 filtered candidates, and 2.875 MB incremental RSS.
- Accelerate and scalar scoring differ by at most 0.00001371 with identical stable top-k ordering. Header/payload validation detects truncation before returning any result. Exact scan passed, so the documented HNSW fallback was not used.
- An intermediate `FileHandle`/`Data` hashing loop retained excessive memory during benchmark sampling; a reusable one-megabyte POSIX read buffer fixed the measurement without weakening full-file SHA-256 verification. A bare newly signed CLI helper was also immediately killed by the OS on this Personal Team setup, so S3/S4 correctly used the already validated signed app container; helper-signing architecture remains in the later helper story rather than being redesigned here.
- MobileCLIP's recorded license permits this private research evaluation but not redistribution or commercial use. Any such release requires a different grant or approved model decision.
- The full 58-test suite, 11 contract fixtures, privacy smoke, universal offline Release build, release-binary audit, and canonical S3/S4 evidence checker pass. Evidence: `Results/LM-007/report.json` and `Benchmarks/Results/S3S4/20260827T193136Z/`.

## 2026-08-28 — LM-008 passed

- S5 validates GRDB 7.11.1 over SQLCipher 4.18.0 with WAL, a random 32-byte Keychain key, 100 raw-file sentinels, five concurrent roles, 10,000 modeled crash points, and four forced process crashes. Encrypted p95 was 9.4265 ms versus 9.345083 ms plaintext, an overhead fraction of 0.00871. No sentinel appeared before deletion, every crash recovered consistently, and the real seeded HEVC fixture decoded before deletion but no matching decodable media remained afterward.
- Separate Personal Team helper executables cannot reliably claim the app's Keychain group. The plan-authorized fallback is fixed in ADR 0002: encrypted CLI/MCP access is provided by `--cli` and `--mcp` modes of the signed app executable. Standalone launchers have no Keychain entitlement, while unsigned and mismatched-access probes both fail with `errSecMissingEntitlement`.
- The pure SwiftUI `LazyVGrid` rehearsal stalled at distant card 5,468 for more than 120 seconds. ADR 0003 applies the authorized narrow fallback: only the 10,000-card hot collection uses `NSCollectionView`; the panel, detail, timeline, state, tokens, and accessibility remain SwiftUI/native. Signed Release XCUITest measured 43.040 ms warm and 260.190 ms cold focus, 10.905 ms initial-render p95, 58.728 fps fast-scroll p95, 129.188 MB maximum RSS, 0.014 ms thumbnail feedback, 18.296 ms full-frame settle, and zero stale screenshots.
- H0 UI Automation was resolved by user approval. The canonical signed XCUITest also passed resize, light/dark appearance, VoiceOver projection, keyboard navigation, and 40%-expanded pseudo-localization without clipped critical controls.
- S7 separates cache-only build closure from deny-all runtime closure because nesting Xcode/SwiftPM's own sandbox inside `sandbox-exec` fails before compilation on this macOS version. Release inputs are materialized from the verified local cache with automatic resolution disabled; the signed app/CLI/MCP journey runs under `deny network*` and recorded zero DNS, TCP, UDP, HTTP, QUIC, or denied shipping attempts. A positive sandbox control recorded one denial, all nine local journey stages completed, and all 27 regular shipping files have complete source/version/license/hash/update provenance.
- MobileCLIP-S0 is now classified as a bundled shipping dependency for this private research build. Its research-only license remains a hard restriction against redistribution or commercial use without a different grant or approved model decision.
- The final gates pass: 57 XCTest unit tests plus 4 Swift Testing unit tests, 7 integration tests, 4 privacy tests, and 1 performance test; 11 contract fixtures; privacy smoke; universal offline-capable Release build; signed posture; release binary audit; and all three canonical spike checkers. Evidence: `Results/LM-008/report.json`, `Benchmarks/Results/S5/20260827T224019Z/`, `Benchmarks/Results/S6/20260827T222621Z/`, and `Benchmarks/Results/S7/20260827T223755Z/`.

## 2026-08-28 — LM-009 passed

- H0 required macOS Developer Mode in addition to the already granted Accessibility permission. After the user enabled it, `/usr/sbin/DevToolsSecurity -status` reported Developer Mode enabled and signed Release XCUITest automation resumed.
- `LocalMemoryAppLifecycle` is an actor-backed, owner-only state machine for recording, paused, permission-required, disk-full, and indexing states. Relaunch restores the runtime state but deliberately never restores main-window visibility; malformed state fails closed to permission-required. The parent directory and state file are verified as modes `0700` and `0600`.
- The app now composes one shared lifecycle model across a native SwiftUI `MenuBarExtra` and singleton `Window` scene. The default menu-bar utility launch has zero main windows. Explicit test/evidence launch modes open the singleton scene through SwiftUI's `openWindow` action, avoiding dependence on remembered scene state while preserving the production no-window launch invariant.
- The menu status uses symbol shape, fill, label, action label, and availability—not color alone—to represent every fake runtime state. The popover exposes foreground-window-only status, pause/resume or recovery action, search, timeline, bounded forgetting, main window, Settings, and Quit.
- `Results/LM-009/LM-009.mov` is a nine-second, app-window-only H.264 recording of synthetic state transitions. The capture script resolves the layer-zero window by process ID and records only that window, so no personal desktop or installed-application content is present.
- The final gates pass: 57 XCTest unit tests plus 9 Swift Testing unit tests, 7 integration tests, 4 privacy tests, 1 performance test, 4 signed Release UI tests, 11 contract fixtures, privacy smoke, universal offline-capable Release build, signed posture, release binary audit, and whitespace validation. Evidence: `Results/LM-009/report.json`.

## 2026-08-28 — LM-010 passed

- The main scene now uses native `NavigationSplitView` and `List(selection:)` behavior with a 1120 × 760 default, 840 × 560 minimum, a 184-point governed sidebar, and a 264-point inspector that disappears below 900 points. The separate native Settings scene is 680 × 560 and exposes Capture, Privacy, Storage, Search, Agents, and About sections.
- Durable navigation restoration is owner-only (`0700` parent and `0600` file), restores both section and selected moment, and fails closed on malformed data. Multiple SwiftUI startup callers share one restoration task and re-check restored state after awaiting it; early menu or command navigation is queued so restoration cannot overwrite explicit user intent.
- Native list selection was required for visible selection and accessibility semantics. Deterministic evidence sizing is applied only after the SwiftUI/AppKit window attachment and restoration pass, preventing scene restoration from racing the requested default or minimum geometry.
- On macOS 26, UI Automation requires an interactive device-owner authentication for the current session. Timed-out clients can remain registered in the user-scoped `testmanagerd` and then stall ordinary XCTest. The safe recovery is to verify that no app, Xcode, or `xctest` process remains, clear only that exact user-scoped daemon, and launch one authenticated test runner.
- Six PID-owned, layer-zero, window-only screenshots cover the main and Settings scenes in light and dark appearances, with the main scene at both default and minimum geometry. All content is synthetic. The full 62-test XCTest unit suite, 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 7 signed Release UI tests, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation pass. Evidence: `Results/LM-010/report.json`.

## 2026-08-28 — LM-011 passed

- `MemoryDesignSystem` now owns the complete plan-11 foundation: 12 semantic color tokens, the 4/8/12/16/24/32 spacing scale, 6/10/14 radii, 28/32/36 control heights, one-physical-pixel hairlines, seven native type roles, and 100/180/260 ms motion roles.
- Colors bridge only to dynamic AppKit semantic sources. The test suite resolves the window token under Aqua and Dark Aqua and proves the values differ; the canonical catalog contains no hex, RGB, calibrated, or light-only color encoding.
- Reduce Motion retains governed timing but replaces movement transitions with opacity. Increased Contrast raises selected-surface opacity from 0.16 to 0.28 and hairlines from one to two physical pixels. `MemoryTokenReader` derives both policies and display scale directly from SwiftUI's system environment.
- `Results/LM-011/tokens.json` is byte-identical to the runtime-generated, sorted canonical snapshot and is enforced by the design-system tests. The full 69-test XCTest unit suite, 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation pass. Evidence: `Results/LM-011/report.json`.

## 2026-08-28 — LM-012 passed

- `MemoryDesignSystem` now owns eleven native SwiftUI shared controls: search field, filter token/bar, capture status badge, result card, evidence snippet, permission row, empty/error/progress states, and destructive confirmation sheet. Feature code can compose these without inventing colors, type, spacing, radii, motion, or destructive-button behavior.
- The deterministic component matrix is the Cartesian product of 11 controls and 13 required variants, producing 143 unique scenarios. It covers normal, hover, pressed, focused, disabled, selected, loading, error, light, dark, Increased Contrast, long German localization, and explicit keyboard focus. A compiled `#Preview` gallery renders the same matrix.
- Search focuses on open and exposes an accessible clear control. Progress remains absent through 300 ms and becomes quiet inline status only after that threshold. Result cards enforce one-line titles, two-line evidence, and announce time, app, host, evidence type, and result position.
- Capture status always combines a unique icon with text and an accessibility label. Recording red remains reserved for the recording state; error chrome uses the warning semantic and destructive emphasis comes from the native destructive button role. Confirmation requires a truthful removal scope and consequence, and Cancel is the default keyboard action.
- `Results/LM-012/component-matrix.json` is byte-identical to the runtime-generated catalog. The full 76-test XCTest unit suite, 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation pass. Evidence: `Results/LM-012/report.json`.

## 2026-08-28 — LM-013 passed

- The snapshot harness uses an offscreen `NSHostingView`, explicit Aqua/Dark Aqua/Accessibility High Contrast Aqua appearance, an explicit 1× or 2× `NSBitmapImageRep`, and disabled animation. It never depends on the current display's backing scale or appearance.
- The canonical matrix contains 12 baselines: default 1120 × 760 and minimum 840 × 560 logical sizes, each at 1×/2× in light, dark, and Increased Contrast. All images are synthetic, local, and manifest-pinned; the total gallery is 2,028,566 bytes.
- Visual review found that the first minimum-size board compressed away search text and truncated the permission recovery action. Search now has layout priority, the permission row uses a narrow-safe vertical action layout, and the compact confirmation density keeps all eleven controls visible at 840 × 560 without clipping.
- `scripts/run-lm013-snapshots.sh --check` renders into a validated temporary directory and requires byte-for-byte equality for every PNG, the manifest, and the offline self-contained HTML index. The final rerender has zero unapproved diffs.
- The full 80-test XCTest unit suite, 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation pass. Evidence: `Results/LM-013/report.json` and `Results/LM-013/snapshot-index.html`.

## 2026-08-28 — LM-014 passed

- First launch now opens one 760 × 620 native four-step onboarding window and persists the exact current step in owner-only state. Completion closes onboarding, opens the main scene, and prevents the wizard from reopening on later launches.
- Screen Recording and Accessibility are the only onboarding permissions. Status is derived from capability probes, including an explicit revoked state; permission actions occur only after `Open System Settings`, and a persisted action ledger proves relaunch never generates another action or prompt.
- The privacy step states the fixed foreground-window-only guarantee verbatim and animates synthetic foreground/background fixtures into a saved-image preview containing only the foreground fixture. The ready step reports the menu-bar control, pause-shortcut posture, local archive, 30-day retention, 20-GB cap, and deliberately distinguishes future database encryption from current macOS/FileVault media protection.
- A `NavigationSplitView` detail can propagate an unbounded vertical proposal even when its outer window has a fixed frame. Bounding both the step region and footer to the 620-point content height keeps Back/Continue/Finish hittable in XCUITest; relying only on an outer frame allowed the footer to be laid out far below the clipped window.
- H0 UI Automation was resolved by approving the stable `.build/LM010DerivedData` runner. The canonical signed suite passed all 10 existing and onboarding UI tests, including denied/relaunch, granted/completion, and revoked/recovery paths. The full 86-test XCTest unit suite, 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation pass. Evidence: `Results/LM-014/report.json` and `Results/LM-014/voiceover.txt`.

## 2026-08-28 — LM-015 passed

- Option-Space is registered as a real system-wide shortcut through Carbon `RegisterEventHotKey`; Settings can switch to Command-Shift-K. Registration collisions produce the stable `LM-SHORTCUT-409` state and one recovery action, and the selected shortcut is persisted with `0700` parent and `0600` file permissions.
- Global search is a native 760 × 620 AppKit `NSPanel` with a 640 × 480 minimum. It uses the non-activating style while explicitly becoming key when invoked, focuses the search field, remembers the last display, and fails closed to a valid pointer/primary display when remembered geometry is unavailable.
- Search and the main scene share one `MainNavigationViewModel`. Selecting a result in the transient panel survives into the main scene, while Escape closes only the panel and preserves navigation state.
- The retained signed Release UI measurement recorded 54 ms cold and 8 ms warm presentation, below the frozen S6 gates of 400 ms and 150 ms. The canonical UI suite also invokes the panel with a real Option-Space event and verifies geometry, focus, filtering, Escape, shared navigation, collision recovery, and owner-only persistence.
- The final gates pass: 93 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 4 signed Release UI tests, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation. Evidence: `Results/LM-015/report.json`.

## 2026-08-28 — LM-016 passed

- The main shell now exposes Search, Timeline, Activity, and Settings through native sidebar selection plus Command-1/2/3/comma. Selection history supports Command-brackets, Command-F moves focus without breaking editable spaces, and the complete plan-11 moment vocabulary is present in native menus.
- A window-scoped AppKit event monitor supplies reliable Space, Return, Command-Return, Option-arrow, Command-Delete, and Escape behavior without stealing default actions from onboarding, Settings, or the global search panel. The first broad regression caught the panel Escape collision; scoping to the main window and attached sheets fixed it and a new onboarding Return regression prevents recurrence.
- Quick Look and confirmation sheets restore the selected-row keyboard path after dismissal. Forget Moment always opens truthful confirmation with Cancel as the default, and no global shortcut silently deletes data or changes recording state.
- Standard empty, delayed-loading, and recoverable-error projections reuse the shared design system. The three icon-only shell controls have labels, unique tooltips, and explicit 24-point minimum targets; semantic system fonts preserve the 11-point minimum body posture.
- Result rows announce time, application, approved host, evidence source, and position. Timeline exposes markers and a permission-loss gap as an accessibility list, while Activity exposes date/hour/recorded/gap values as a table projection.
- English and 40%-expanded pseudo-localized accessibility projections pass. Clock, named relative-day, and week-start behavior is verified through Foundation under `en_US` and `fr_FR`.
- The final gates pass: 101 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 20 signed Release UI tests, 11 contracts, the retained accessibility audit, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation. Evidence: `Results/LM-016/report.json` and `Results/LM-016/a11y.txt`.

## 2026-08-28 — LM-017 passed

- `ArchiveDatabase` now owns the application-lifetime GRDB writer. Production opens `database/archive.sqlite3` through a five-reader `DatabasePool` in WAL mode with foreign keys, full synchronous writes, a five-second busy timeout, file-backed temporary storage beside the database, and `0600` database/WAL/SHM permissions inside the existing owner-only archive tree.
- The append-only `v1_archive_schema` migration creates all 13 plan-10 logical tables and 12 query/scheduling indexes. Frame-owned text spans, artifacts, and vector offsets cascade; foreign-key integrity is exercised after a real deletion.
- `frame_fts` is an FTS5 external-content index over `frames.approved_text`, title, app, host, and path. The approved merged-text projection is database-only, and no implicit trigger exists: later enrichment/search work must maintain the index explicitly inside observable transactions.
- Forced interruption after all V1 statements but before migration commit rolls the complete logical schema back, leaves no applied identifier, and then resumes exactly once. A version-zero fixture remains readable across migration, while two deterministic in-memory stores produce byte-identical schema and metadata.
- `Results/LM-017/schema.sql` is exported from a freshly migrated store and validated against all canonical tables, external-content configuration, and the no-trigger invariant. The final gates pass: 108 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation. Evidence: `Results/LM-017/report.json` and `Results/LM-017/schema.sql`.

## 2026-08-28 — LM-018 passed

- `ArchiveRelativePath` validates both construction and decoding, accepts only six managed archive roots, and rejects absolute, tilde, backslash, NUL, dot, parent, empty, partial-suffix, unmanaged-root, and symlink-escape paths before any file operation.
- `ArchiveFileStore` writes an owner-only sibling partial without overwriting, synchronizes it, computes its SHA-256 and byte count, atomically promotes it, synchronizes the parent directory, and only then invokes the database commit. Ordinary failures clean the partial while the two injected crash boundaries preserve exactly the artifact startup recovery must reconcile.
- Startup recovery removes orphan partials, quarantines uncommitted or corrupt completed files, suppresses database and FTS projections for missing or invalid ready media, cancels related artifact work, requeues interrupted leases, and recursively repairs the archive tree to `0700` directories and `0600` files.
- The production LM-018 harness proves before-rename and after-rename/before-commit recovery, SHA-256 mismatch and missing-file quarantine, zero searchable corruption, traversal rejection, lease recovery, and owner-only permissions. The final gates pass: 117 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation. Evidence: `Results/LM-018/report.json` and `Results/LM-018/faults.json`.

## 2026-08-28 — LM-019 passed

- Screen recording permission is probed without requesting it. Shareable-content enumeration is a two-second bounded lease: retries stay suppressed for 30 seconds after timeout, a replacement lease may then begin, and a late completion from the old lease cannot clear or mutate the replacement.
- The lifecycle actor starts with no eligible target and therefore creates no stream. Permission loss, refresh failure, target disappearance, or ineligibility stops delivery; a stable target retains one long-lived stream, while a display change stops, refreshes, and restarts only that same eligible target.
- The production driver has only a `desktopIndependentWindow` filter construction. Its output adapter can create only foreground-window pixel-buffer leases, which strongly retain the in-process `CVPixelBuffer` until a media-commit closure or explicit non-persistent discard consumes the lease exactly once.
- The signed Release harness used the stable app identity and real TCC grant, refreshed real `SCShareableContent`, selected only the app's own eligible window, received one frame, restarted the stream for a display lifecycle change, received another frame, released both leases, persisted zero frames, and returned to no-target. The final gates pass: 126 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation. Evidence: `Results/LM-019/report.json` and `Results/LM-019/lifecycle.json`.

## 2026-08-28 — LM-020 passed

- `NSWorkspaceForegroundApplicationMonitor` emits a monotonically sequenced initial state and foreground lifecycle transitions. Accessibility is inspected only after the frontmost application identity is known, with a 50 ms messaging timeout and public AX attributes; no private window-ID bridge or composited-display capture API exists in the shipping sources.
- A public AX top-level attribute is not universally exposed by SwiftUI windows. The signed probe therefore records the exact identity source: it prefers and cross-checks focused plus top-level windows when both exist, while a PID-validated focused `AXWindow` with role, subrole, bounds, and title is the public fallback. The resolver still requires AX/SCK PID and normalized geometry agreement before approval.
- `ForegroundCaptureTargetResolver` refreshes shareable content at most once per foreground transition, caches only that transition's result, and invalidates immediately when a selected window disappears. It returns the actual `SCWindow` only after strict identity and eligibility checks; every gap carries metadata and zero pixel payloads.
- The frozen 500-transition corpus resolves all 350 eligible cases to the exact window ID and emits 25 each of ambiguous, minimized, no-window, protected, unresolved, and unsupported-display gaps. All 150 gaps contain zero pixels. A signed real app probe additionally matched the NSWorkspace foreground PID, AX PID, and resolved SCK PID on the main display.
- The final gates pass: 131 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 5 targeted resolver tests, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, forbidden-API scan, and whitespace validation. Evidence: `Results/LM-020/report.json` and `Results/LM-020/resolver.json`.

## 2026-08-28 — LM-021 passed

- Five explicit adapter definitions cover Safari, Chrome, Arc/Dia-family Chromium, Edge, and Firefox through exact bundle-identifier allowlists and public Accessibility address/document attributes. The production path has no DOM, WebKit, browser-history database, network-traffic, or private-API access.
- Browser context is accepted only after the already-resolved SCK target PID matches the observed AX window PID and normalized bounds. Duplicate candidate address fields, bundle lookalikes, PID/bounds mismatches, unsupported schemes or ports, missing URLs, and unknown private state all return typed unavailable results.
- URL normalization accepts only HTTP/HTTPS with a default port, lowercases scheme and host, retains only a validated absolute path, and constructs serialization from sanitized components. It never reuses the source URL string, so credentials, query, and fragment components cannot cross the serialization boundary.
- Private classification is content-free: it contains only the browser family, resolved target-window ID, and private flag. The system inspector returns before reading address fields for private or uncertain windows, and no private title or URL is exposed by the output.
- The signed 600-context matrix achieves 1.0 exact accuracy: 480 approved sanitized contexts, 30 content-free private classifications, and 90 typed unavailable cases. The final gates pass: 138 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 7 targeted browser tests, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, forbidden-source scan, and whitespace validation. Evidence: `Results/LM-021/report.json` and `Results/LM-021/browser-context.json`.

## 2026-08-28 — LM-022 passed

- `PrivacyPolicyConfiguration` derives the self/login/permission fixed set instead of serializing it as editable state, so decoding or rule replacement cannot remove those exclusions. Versioned password-manager exclusions and private-browser handling remain explicit editable defaults.
- The actor compiles normalized application/exact-host/suffix-host rules once per policy generation. Fixed exclusions, password-manager defaults, private handling, and capture uncertainty precede ordered user rules; the final matching user rule wins only after those higher-priority checks.
- Prefilter approval captures the exact window ID, PID, bundle, browser resolution, capture epoch, and policy generation. The immediate final recheck denies target/context or epoch drift and invalidates every approval after a policy replacement.
- Supported browsers fail closed when context is absent, ambiguous, private-unknown, target-mismatched, or otherwise unavailable. Denied/private/uncertain decisions scrub bundle and host from audit rows, and the store rejects URL/path/query-shaped audit injections.
- The signed 1,000-case property corpus resolves 200 each of ambiguous target, missing target, private context, denied site, and unavailable URL context exactly. All 1,000 prohibited sentinel scenes project zero pixels, text, derived artifacts, and cache payloads; their 1,000 persisted policy rows expose zero content fields or denied-context fields.
- The final gates pass: 149 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 targeted policy tests, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, and whitespace validation. Evidence: `Results/LM-022/report.json` and `Results/LM-022/privacy-policy.json`.

## 2026-08-28 — LM-023 passed

- `ActivityMonitor` stores only one of three coarse input classes—click, scroll, or key activity—plus monotonic time. It rejects out-of-order signals and never reads key codes, characters, clipboard contents, cursor locations, or input payload fields.
- The fake-clock boundary is exact: activity is active for one second, recently active until five minutes, and idle at five minutes. Idle rejects frame acceptance; fresh activity or the already-authorized target-change path can recover it.
- Sleep and session lock independently suspend activity and ignore input. Wake or unlock removes only its matching suspension and deliberately remains idle until a fresh input; sleep takes precedence when both suspensions are present.
- The shipping adapters use a listen-only CGEvent tap and the four public NSWorkspace lifecycle notifications. Observer removal, event-tap invalidation, duplicate lifecycle notifications, and disabled-tap recovery are explicit.
- The signed 15-transition harness and nine focused tests pass with zero sensitive payload fields. The final gates pass: 158 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, source/binary payload scans, and whitespace validation. Evidence: `Results/LM-023/report.json` and `Results/LM-023/activity-monitor.json`.

## 2026-08-28 — LM-024 passed

- `WindowCaptureEpochManager` revokes before awaiting a filter update, serializes concurrent updates, and installs an epoch only when the latest receipt exactly matches its generation, target, and encoded dimensions. Failed or superseded completions leave no active epoch.
- `ScreenCaptureKitEpochStream` owns one long-lived stream and constructs only `SCContentFilter(desktopIndependentWindow:)`. It removes the prior output adapter during updates; immutable adapters tag every queued callback with their filter generation, so a late callback cannot masquerade as the new epoch.
- Frame admission now rejects revoked epochs, old filter generations, pre-filter buffers, target/focus drift, policy-decision drift, and resize mismatches. The final gate repeats LM-022's policy fingerprint immediately before append rather than trusting an earlier boolean.
- Deduplication uses the measured mean absolute difference between 4,096 luminance samples. The frozen threshold of 2.0 deduplicates all 50 near-duplicate sequences while detecting all 50 material changes; the existing first-frame, 1 fps active, 2-second indexing, 30-second heartbeat, and five-minute idle cadences remain intact.
- The capacity-four capture-to-media queue flushes prior epochs, rejects late stale arrivals, drops redundant heartbeats first, then the oldest candidate, and retains the newest frame under a 100-frame overload.
- The signed 50-race matrix accepts zero stale frames across focus, filter, URL-policy, and resize changes; all 50 correct frames begin under one second with 57 ms p95. The final gates pass: 168 XCTest unit tests plus 9 Swift Testing tests, 7 integration tests, 4 privacy tests, 1 performance test, 10 focused epoch tests, 11 contracts, privacy smoke, universal offline-capable Release build, signing posture, release-binary audit, single-window source/binary scans, and whitespace validation. Evidence: `Results/LM-024/report.json` and `Results/LM-024/epoch-capture.json`.

## 2026-08-28 — LM-025 blocked: hardware media quarantine

- Two focused runs of the new hardware-required HEVC writer caused repeatable `dart-ave` AppleT8110DART kernel panics at 14:43 and 14:49 local time. The second same-size 320x180 run proved downscaling was not required to trigger the failure. Both runs shared the draft `AVAssetWriterInputPixelBufferAdaptor` path, while the earlier passed S1 writer appended `CMSampleBuffer` directly. Diagnostic hashes and compact signatures are in `Results/LM-025/panic-analysis.json`; no reproduction was attempted after quarantine.
- Safe recovery extracted `MediaWriterCore` and `MediaChunkPublisher`, restored the direct same-size sample-buffer append path, restricted pixel transfer to required downscale, and added a pre-construction skip across hardware media integration tests. The five-scenario pure harness proves scoped epoch/target/dimensions, aspect-preserving downscale plans, VFR locators and 30-second cap, mock backpressure commits, owner-only atomic publish, SHA-256, and rename fault boundaries without importing AVFoundation or VideoToolbox.
- The pure harness, source scan, changed-source format checks, whitespace validation, and package compile-only build pass. Real hardware encode, playable VFR duration/PTS/frame count, random decode, forced-termination playability, and runtime hardware-acceleration confirmation remain unverified. The first three documented S1 tuning steps still execute the quarantined hardware path; the HEIC fallback requires an ADR. Resume only with explicit safe-host/OS reauthorization or an architecture decision for that fallback. Evidence: `Results/LM-025/report.json` and `Results/LM-025/quarantine.md`.

## 2026-08-28 — LM-025 blocked checkpoint refined

- The explicit buffer-ownership correction now copies every same-size or downscaled source into `AVAssetWriterInputPixelBufferAdaptor.pixelBufferPool`, appends only through that adaptor, and holds each accepted pool buffer in `MediaBufferRetentionLedger` until finish, failure, cancellation, or deinitialization completes. A RED compile captured the missing retention interface; the GREEN pure test proves caller-released objects remain alive until ledger finalization and cannot be accepted afterward.
- The safe harness now uses a deterministic software fixture encoder, verifies exact SHA-256 and owner-only atomic publication, covers both rename fault boundaries, and kills a real non-VideoToolbox child process after a synchronized mid-write partial. The partial remains recoverable and no final path becomes visible. All six scenarios, static adaptor/no-bypass audit, package compile-only build, format, and whitespace checks pass.
- Hardware HEVC, AVAssetWriter media execution, random decode, and playable forced-termination fixtures remain quarantined and unexecuted. The two panic fingerprints remain the technical blocker; LM-025 is not passed and has no checkpoint tag. Evidence: `Results/LM-025/report.json`, `Results/LM-025/software-termination.txt`, and `Results/LM-025/panic-analysis.json`.

## 2026-08-28 — LM-025 passed with the authorized HEIC-keyframe architecture

- ADR 0001 now adopts independently encoded HEIC files grouped in immutable logical chunk directories. Canonical V2 manifests bind chunk, epoch, target window, fixed dimensions, ordered frame identities, exact paths, byte counts, and per-frame SHA-256; searchable frames cite the exact source asset. Legacy V1 HEVC fixtures remain read-compatible only.
- The complete hidden directory publishes through synchronized per-file no-replace renames, a manifest written last, full inventory verification, one no-replace directory rename, and parent-directory synchronization. Retained-only deletion republishes unchanged allowed frames and proves the removed sentinel and identity are absent before retiring the old directory.
- Safe boundary-fake, fault-injection, subprocess-termination, V1/V2 contract, privacy, compile-only Release, format, and source-audit gates pass under a live encoder-process tripwire. Production ImageIO HEIC is compile-only in this story. No AVAssetWriter, VideoToolbox, or hardware HEVC encoder path ran, and the two `dart-ave` panic artifacts remain permanently quarantined on this Mac. Evidence: `Results/LM-025/report.json` and `Results/LM-025/architecture-evaluation.json`.

## 2026-08-28 — LM-026 passed

- The append-only `v2_heic_frame_locators` migration preserves legacy V1 rows and adds exact source path, per-frame digest/byte count, and final policy generation. Insert/update triggers reject incomplete, traversal-like, staging, or identity-inconsistent V2 locators.
- `ArchiveAtomicCoordinator` validates the complete canonical manifest/assets twice, repeats epoch/target/policy identity inside the GRDB write transaction, and commits the ready chunk, ordered frame rows, and retryable jobs as one state. Denial, identity drift, after-verification failure, and mid-transaction failure expose zero rows; an after-commit failure reopens as the complete state.
- Startup removes staging directories, deterministically quarantines published orphan directories, and revalidates manifest identity, inventory, chunk/frame hashes, exact locators, and policy generation. Corrupt media becomes nonsearchable and dependent jobs are cancelled. Twenty-four focused Release tests, V1/V2 contracts, privacy smoke, strict format, and universal Release compile pass under the encoder tripwire; zero app launches or hardware encoder tests ran. Evidence: `Results/LM-026/report.json` and `Results/LM-026/state-machine.json`.

## 2026-08-28 — LM-036 passed

- `SearchQueryParser` implements a deterministic, Foundation-only, quote-aware grammar for explicit application and site filters, ISO and locale dates, half-open `before:`/`after:` bounds, and localized today, yesterday, and last-week phrases. Unknown applications, invalid sites, malformed operators, and ambiguous time language remain in the lexical query.
- The committed corpus contains 100 unique goldens: ten each for lexical text, quotations, today, yesterday, last week, applications, sites, explicit bounds, explicit dates, and ambiguity. It covers `en_US`, `fr_FR`, `de_DE`, and `es_ES`, repeats every parse to prove determinism, and asserts that every structured application/site/time filter has a visible removable token.
- Eight MemorySearch package tests, independent fixture regeneration, contract checks, privacy smoke, strict formatting, whitespace validation, and a universal compile-only Release build pass. Static safety guards prove the package has no AVFoundation, VideoToolbox, AVAssetWriter, or HEVC writer references and that no unit test constructs `HEVCMediaWriter`; zero hardware encoder tests ran. Evidence: `Results/LM-036/report.json` and `Results/LM-036/query-parser.json`.

## 2026-08-28 — LM-056 passed

- Privacy Settings now persists owner-only application and site exclusions, exposes explicit ordered-rule precedence, supports accessible reorder/remove controls, previews the exact matched rule for a synthetic context, and replaces the live policy generation only after durable validation. A pre-change approval can never survive that reload.
- The deterministic 100-case change-during-capture corpus records the exact effective monotonic time for every exclusion. All 100 stale approvals fail their final recheck, zero artifacts persist, zero application sentinels reach app/helper projections, and every fresh denial becomes a content-free excluded gap with no approved bundle identifier. Direct attempts to project an allowed decision as a gap fail closed.
- The timeline exposes all 14 canonical typed gap reasons and the excluded label explicitly states that no application details are stored. The new native SwiftUI rule row follows the shared component conventions with semantic controls, bounded text, accessibility labels/identifiers, and normal/long previews.
- During the first focused XCUITest accessibility wait, macOS unexpectedly spawned `VTEncoderXPCService` even though the LM-056 test and changed sources contain no media symbols. The app, test runner, build, and encoder were stopped before the first case completed; no panic occurred. UI runtime is quarantined, and the passing gate uses static accessibility checks, eight source-isolated unit tests, contract/privacy checks, and a universal compile-only Release build under an encoder-process tripwire. No hardware encoder test ran. Evidence: `Results/LM-056/report.json` and `Results/LM-056/ui-runtime-quarantine.txt`.

## 2026-08-28 — LM-057 passed

- `ProtectedBrowserContextCoordinator` composes the five-browser adapter allowlist with nonprompting permission health, private-window policy, protected-site requirements, and sanitized browser-version tracking. Screen Recording or Accessibility denial/revocation, missing protected URL context, unsupported/missing versions, and version transitions all fail closed with content-free typed gaps and a user-visible reason.
- The private-browser matrix defaults to exclusion, may allow only a content-free private classification when no site rules require URL context, and still fails closed when private URL context would be needed. The persisted setting replaces the live policy generation immediately; stale approvals cannot survive the change.
- The frozen 200-case corpus covers five browser families and 40 cases each of healthy, private, URL-unavailable, permission-revoked, and version-changed behavior. All 160 blocked cases project zero content fields and the exposure gate releases none of their sentinel payloads.
- Privacy Settings exposes accessible private-window handling and a nonprompting permission-health refresh with explicit protected-context status. Runtime UI remains quarantined after the LM-056 encoder-service incident; static UI checks, 26 source-isolated unit tests, contracts, privacy smoke, strict formatting, and a universal compile-only Release build pass under the encoder tripwire. No app launch or hardware encoder test ran. Evidence: `Results/LM-057/report.json` and `Fixtures/LM057/browser-protection-matrix.json`.

## 2026-08-28 — LM-059 passed

- Every file-backed `ArchiveDatabase` now requires an explicit 256-bit key, applies the SQLCipher passphrase before other pragmas on every pooled connection, enables cipher memory security, confines file-backed temporary state beside the database, and verifies both cipher activation and cipher integrity on each open. Deterministic, migration, and fault fixtures are encrypted too; plaintext database construction remains isolated to the S5 comparison benchmark.
- `ArchiveKeyManager` retrieves the fixed primary-archive account from the signed app's shared Keychain group and generates only when the entire archive is fresh. A missing or invalid key beside any database sidecar or other archive file fails closed and never silently replaces the key. The prior signed S5 evidence still proves embedded CLI/MCP modes share the key while unsigned and mismatched-group probes are denied; standalone helper entitlements remain empty.
- Unrecoverable encryption state no longer terminates the app. Storage Settings shows that recording is stopped, explains the SQLCipher versus FileVault boundary, and requires the exact phrase `DELETE LOCAL MEMORY ARCHIVE` before atomically staging and deleting the complete archive and Keychain item. Five near-miss confirmations produce no side effect; Keychain-delete failure restores the staged archive; a completed reset creates a distinct fresh key and verified encrypted database.
- The Release S5 rerun scans 100 sentinels across three database/WAL files with zero plaintext or key matches, completes 1,000 concurrent operations with zero failures, and measures 10.478 ms encrypted p95 versus 9.182 ms plaintext p95—14.1% overhead under the fixed 20% ceiling. Twenty-seven focused tests, prior signed ACL validation, contracts, privacy smoke, strict formatting, and compile-only universal Release pass under the encoder tripwire. No app launch or hardware encoder test ran. Evidence: `Results/LM-059/report.json` and `Results/LM-059/storage-security.json`.

## 2026-08-28 — LM-027 passed

- `CaptureLifecycleCoordinator` is the only capture-admission projection. It admits frames only when user recording, process, encrypted archive, storage, filter, permission, activity, exact resolver target, and running stream identity are all healthy; a stale running-window ID becomes `unresolvedWindow` with no active target.
- The menu and Capture Settings now expose recording, paused, idle, sleep/lock, permission, low-disk, target-unavailable, filter/archive/process-stop causes while keeping `Foreground window only` visible. The pause/resume command mutates coordinator inputs rather than only changing UI state, and fake-clock projection remains within the 250 ms contract.
- Completed intervals persist into encrypted `activity_intervals` with no application identity. All six resolver gaps retain exact types; low disk, archive failure, and process interruption retain exact live causes within the canonical `processStopped` timeline class. A recording/indexing relaunch fails closed, restores the open gap from the last lifecycle timestamp, and closes it only after full healthy reconciliation.
- Launch at login defaults off and only the Settings toggle may call `SMAppService.register()` or `unregister()`. A fake `requiresApproval` result projects the H3 Login Items instruction without pretending the service is enabled. No real registration occurred, so H3 was not triggered.
- Thirty-five focused Release XCTest/Swift Testing cases across lifecycle, activity, resolver, and fake stream control, plus contracts, privacy smoke, dependency audit, strict format, and universal Release compile pass under the encoder-process tripwire. No app runtime or hardware encoder test ran. Evidence: `Results/LM-027/report.json` and `Results/LM-027/lifecycle.json`.

## 2026-08-28 — LM-028 safe suite passed; production soak blocked

- A metadata-only `sips` probe of a pinned HEIC fixture unexpectedly started `VTEncoderXPCService`. The tripwire caught it, the service was terminated immediately, no Xcode/test process remained, and no further Apple ImageIO runtime probe ran. Apple ImageIO encode and decode are both quarantined on this Mac.
- The HEIC representation remains sound, but unsafe implicit reachability did not: `HEICKeyframeWriter` now requires explicit encoder injection, the legacy capture harness defaults to `QuarantinedHEICFrameEncoder`, and the old ImageIO decode benchmark fails closed before opening media.
- Sixty-seven focused Release tests pass across soak modeling, focus/filter races, privacy projection, lifecycle, resolver, archive faults/recovery, and fake media publication. Contracts, privacy smoke, dependency audit, strict format, and universal Release compile also pass with zero app launches and zero hardware encoder tests.
- A pinned, licensed three-file libheif corpus decoded only through FFmpeg software HEVC (`-hwaccel none`, one thread) has a worst p95 of 85.664 ms and 32.473 MB peak child RSS. Its 10% high-entropy/90% UI weighted projection is 197,487 bytes at 1920×1080.
- The eight-hour office model processes 57,600 candidates, persists 3,108 frames, performs 10 recoveries, peaks at the fixed four-frame queue, ends empty, and projects 18.414 GB over 30 eight-hour days. The 72-hour accelerated model processes 518,400 candidates with the same bounded/empty queue and zero modeled prohibited, corrupt, or orphan publication.
- LM-028 remains technically blocked only on the real wall-clock production HEIC capture/decode/resource soak; synthetic time and software decode evidence are not mislabeled as that gate. Evidence: `Results/LM-028/soak-report.md` and `Results/LM-028/blocker.md`.

## 2026-08-28 — LM-029 passed

- Accessibility extraction is now capture-owned and scoped to the already accepted capture epoch, ScreenCaptureKit window ID, foreground PID, normalized bounds, and title. PID, geometry, or title drift returns a content-free `targetMismatch` rather than traversing a substitute window.
- Traversal is capped at 256 nodes, depth 12, 512 characters per field, and a 45 ms end-to-end deadline. Production child enumeration uses bounded AX range reads, so a huge child list is never materialized before the node budget applies; timeout returns zero partial content.
- Only 19 explicit text-bearing roles are projected. Secure-field values are short-circuited before the AX value getter is evaluated, while hierarchy paths and content-free role/subrole/identifier signatures remain available for downstream normalization.
- Raw AX node interfaces and immutable projections are intentionally non-`Codable`, no Store/GRDB dependency exists, raw system objects remain inside an autorelease-scoped background operation, and the capture callback receives an asynchronous result without waiting on AX messaging.
- Sixteen focused Release tests pass across the LM-029 extractor, prior S2 projection, and exact foreground resolver. The canonical 250-frame corpus reaches 100% useful coverage with 0.001084 ms p95, under the 80% and 50 ms gates. Contracts, privacy smoke, dependency audit, strict formatting, and universal compile-only Release pass under the encoder tripwire; no app, system AX probe, Apple ImageIO runtime, or hardware encoder executed. Evidence: `Results/LM-029/report.json`.

## 2026-08-28 — LM-030 passed

- `AccessibilityTextSpanEmitter` consumes only LM-029's transient projection, sorts it by hierarchy path and content-free signature, canonicalizes each title/value/description/help string through the locked `TextSpan` contract, and emits Accessibility provenance with no OCR confidence or inferred language.
- Secure-field values are independently suppressed again during emission. Element identifiers and signatures are never searchable inputs, and a fixture placing the same prohibited sentinel in value, identifier, and signature produces zero sentinel bytes in encoded spans.
- Element bounds are clipped to the accepted foreground window and converted to validated upper-left-origin `NormalizedRect` coordinates. Invalid, nonfinite, nonpositive, or fully outside rectangles safely become unplaced spans rather than invalid contract rows.
- Duplicate canonical text collapses when normalized bounds overlap at IoU ≥0.5 or both spans lack bounds. Identical text in distinct screen positions remains independently highlightable; traversal ordering and injected IDs remain deterministic across input insertion order.
- Twelve byte-pinned multilingual normalization goldens cover Unicode composition, ASCII/Unicode whitespace, CRLF, French, German, Spanish, CJK, and emoji. Twenty-five focused Release tests across emission, extraction, and V1 contracts pass, as do contracts, privacy smoke, dependency audit, strict format, and universal compile-only Release under the encoder tripwire. No app, system AX probe, Apple ImageIO runtime, or hardware encoder executed. Evidence: `Results/LM-030/report.json`.

## 2026-08-28 — LM-031 passed

- `VisionOCRPipeline` accepts explicit eight-way image orientation plus source dimensions and a processed crop. Vision's lower-left normalized boxes are scale-corrected into validated upper-left source-window `NormalizedRect`s before `visionOCR` `TextSpan` emission; confidence and bounded language provenance are retained.
- `AppleVisionTextRecognizer` runs accurate, local-only recognition on a detached utility task and never enters capture callbacks. The 200-frame canonical in-memory CoreGraphics corpus reaches 1.0 high-contrast Latin and 1.0 full recall with 100.023 ms p95 off the capture path.
- `VisionOCRWorker` permits one active job, requires an injected atomic lease store, uses a 120-second lease, defers under serious/critical thermal pressure, and checks cancellation before any publication. Concurrent work is visibly deferred as busy.
- Failures retry below the locked three-attempt limit and become permanent at the limit. Both states expose honest user copy; permanent failure explicitly preserves the capture without claiming OCR text.
- The prior PNG serialization helper now fails closed with `imageIORuntimeQuarantined`; the canonical run used no files and made zero ImageIO encode/decode calls. Seven focused unit/integration tests, contracts, privacy smoke, dependency audit, strict format, and universal compile-only Release passed under the encoder tripwire with no app launch or hardware encoder test. Evidence: `Results/LM-031/report.json`.

## 2026-08-28 — LM-032 passed

- `AXOCRSpanMerger` accepts only validated durable `TextSpan` projections for the exact frame and declared source. It removes suppressed content, gives Accessibility precedence over overlapping normalized OCR duplicates, deterministically selects the highest-confidence duplicate OCR observation, and retains identical text at distinct positions.
- Ordering is stable by bounds, normalized text, provenance, and UUID. The canonical 200-fixture corpus produces 800 merged tokens from 800 unique ground-truth tokens with zero duplicates, zero loss, and byte-identical output under reversed insertion order.
- `ApprovedSearchMetadataProjector` emits normalized bundle/app/title plus only an approved HTTP(S) origin and path. Process identity and raw URL observations are not output fields; private contexts and non-HTTP(S) schemes fail closed, while decoded projections are revalidated against query, fragment, credential, and field-coherence rules.
- Twenty-three focused Release tests across the production merger, AX emission, OCR projection, and S2 spike behavior pass. Contracts, privacy smoke, dependency audit, strict formatting, and universal compile-only Release also pass under the encoder tripwire; no app, Apple ImageIO runtime, hardware HEVC, or VideoToolbox execution occurred. Evidence: `Results/LM-032/report.json`.

## 2026-08-28 — LM-033 safe implementation passed; real HEIC codec gate blocked

- `ThumbnailRasterizer` is a pure in-memory RGBA pipeline covering all eight orientations, Display-P3 and linear-sRGB conversion into tagged sRGB, aspect-preserving bilinear reduction to a 480-pixel long edge, and no upscaling. Pinned pixel-order, aspect, color, and alpha fixtures pass.
- `ThumbnailGenerator` accepts only exact `media/.../frames/<frame-id>.heic` sources with a matching SHA-256, and requires explicitly injected decode and encode boundaries. Runtime adapters default to explicit quarantine rather than reaching ImageIO implicitly.
- Encoded thumbnail bytes are hashed and atomically published through `ArchiveFileStore` under a deterministic UTC path with mode `0600`; the ready artifact binds frame, producer, locator, byte length, and digest. Verified existing files are reused without decode, missing files rebuild deterministically, and deletion verifies the digest before removing the artifact.
- Fourteen focused Release tests across the thumbnail and atomic file stores, plus contracts, privacy smoke, dependency audit, strict formatting, and universal compile-only Release, pass under the encoder tripwire. No app, Apple ImageIO runtime, HEVC, or VideoToolbox path ran.
- LM-033 remains technically blocked only on production fidelity against real HEIC decode/encode bytes. Running that gate here is prohibited after the `sips`-spawned encoder service and two prior hardware-video kernel panics. The exact resume probe is in `Results/LM-033/blocker.md`.

## 2026-08-28 — LM-025 amended and LM-033 passed with pinned software HEIC

- ADR 0001 now adopts a reproducible arm64/macOS-15 codec helper built from pinned libheif 1.23.2, x265 4.3, and libde265 1.1.1 sources. The exact 2,273,651-byte archive reproduced across two clean builds at SHA-256 `ff359032e30591fce5614a60ca0f9be4e0f9e06acea7b64981051d0f867a5e50`; every bundled file is checked again before each invocation.
- The complete helper/dylib closure links no ImageIO, AVFoundation, MediaToolbox, VideoToolbox, or network framework. Real encode/decode tests ran under the encoder-service tripwire without starting `VTEncoderXPCService`; no hardware HEVC, AVAssetWriter, ImageIO runtime, or app path ran. The two `dart-ave` panic paths remain permanently quarantined.
- Production capture now converts only the already admitted foreground epoch's fixed-dimension bi-planar buffer into RGBA and injects the software encoder explicitly. Manifest identity, exact source evidence, atomic directory publication, retained-only replacement, retention, and forensic deletion contracts are unchanged. Fault tests retain the deterministic fake boundary.
- The local helper accepts only bounded owner-only interchange files in a unique mode-`0700` directory, creates mode-`0600` outputs, emits content-free typed failures, and is killed on timeout. Runtime tamper, extra inventory, malformed input, capture conversion, genuine HEIC round-trip, and linked-binary gates pass.
- LM-033's real 480-pixel thumbnail path now decodes transformations through libheif, produces upright sRGB rasters, re-encodes genuine HEIC, verifies source/output hashes, publishes atomically at mode `0600`, reuses verified artifacts, rebuilds missing files, and verifies deletion. Ten focused Release codec/thumbnail tests plus contracts, privacy, dependency, universal Release, formatting, and process-tripwire gates pass. Evidence: `Results/LM-033/software-codec.json`, `Results/LM-033/report.json`, and `Results/LM-025/software-codec-amendment.json`.

## 2026-08-28 — LM-034 durable enrichment scheduler

- `ArchiveEnrichmentJobStore` leases eligible SQLite work atomically for 120 seconds in priority-descending, stable-ID order and increments the attempt before execution. Completion, retry, cancellation, and permanent failure match the exact job, attempt, producer version, and lease expiry, so expired or restarted workers cannot publish stale results.
- Startup and measured-backlog recovery preserve prior attempts while requeueing abandoned leases. Same-process expiry becomes an immediately eligible retry until the third expired attempt, which becomes permanent. Operation failures use bounded 5/10-second retry delays and become `permanentFailure` on attempt three with the content-free `enrichment_failed` code.
- Producer-version synchronization requeues every non-cancelled mismatched job exactly once with a reset attempt budget; repeated synchronization is a no-op. Cancelled policy/deletion work is never resurrected. Atomic backlog snapshots report ready, leased, future retry, success, terminal failure, cancellation, and per-kind counts.
- Capture-transition priority is 1000 versus heartbeat priority 100. Capture transitions preempt all enrichment; non-idle activity defers work below 500, and serious thermal state, Low Power Mode, or battery at/below 20% defers work below 700. Critical thermal state pauses all enrichment. Deferral never acquires a lease or consumes an attempt.
- The menu-bar projection shows the exact atomic pending/failure count with a bounded accessibility value. Twenty-six focused Release scheduler/database/OCR tests, including encrypted archive reopen, plus contracts, privacy, dependency, strict formatting, and universal Release build passed under the 50 ms encoder tripwire. No application or media runtime launched. Evidence: `Results/LM-034/report.json` and `Results/LM-034/story-gate.txt`.

## 2026-08-28 — LM-035 merged text and external-content FTS

- Database schema V3 adds `merged_text_records` as the single external-content authority for approved screen text, title, application, host, path, and a separately reserved transcript field. The serialized capture/CLI/MCP contract remains V2, while legacy ready V2 text migrates with explicit provenance.
- The V3 migration also corrects a latent persistence mismatch: `TextSpan` permits absent bounds and Accessibility confidence, so those SQLite columns are now nullable. Validated non-suppressed Accessibility/OCR spans form deterministic approved text; transcript spans remain isolated and raw AX/Vision observations cannot cross the publication boundary.
- Publish replaces durable spans, merged content, frame text state, and FTS evidence in one GRDB transaction. Updates send exact old values through the FTS5 external-content `delete` command before replacement. Frame deletion removes FTS evidence before foreign-key cascade, and a deliberately induced mid-transaction primary-key collision proved the old spans, record, and search terms roll back intact.
- Rebuild uses `delete-all` and stable row-order insertion from records whose frame and parent media are ready. Integrity compares ready frames and merged records with `frame_fts_docsize` row IDs, so missing/stale index rows cannot be hidden by external-content query passthrough. Startup corruption quarantine explicitly removes FTS evidence and suppresses merged metadata.
- Twenty-nine safe focused Release tests cover schema migration, encrypted reopen/recovery, deterministic merge inputs, all six searchable fields, update stale-term absence, atomic rollback, forensic deletion, rebuild repair, ready-frame parity, and suppressed/wrong-source rejection. Contracts, privacy, strict formatting, universal compile-only Release, and binary dependency gates pass under the 50 ms encoder tripwire; no application, ImageIO, AVAssetWriter, HEVC, or VideoToolbox runtime executed. Evidence: `Results/LM-035/report.json` and `Results/LM-035/story-gate.txt`.

## 2026-08-28 — LM-037 lexical SearchEngine

- `LexicalSearchEngine` executes against LM-035's canonical merged authority through a bounded MemoryStore query API. User input is always quoted as literal FTS5 terms—quoted phrases remain phrases and normalized terms combine with `AND`—so operator-like input cannot broaden a query.
- Hard half-open interval, application, and canonical browser-host predicates enforce the user-created `AccessPolicy` before any result projection. Empty application allowlists return none; browser rows require an allowed canonical host. Expiry is checked on every page and the policy-wide `maxResults` cap is carried cumulatively across pagination.
- Weighted BM25 uses fixed six-field weights `6/4/2/2/1.5/1` with deterministic title/application/host/path boosts. Final order is score descending, capture time descending, then lowercase UUID ascending. Fifty equal-score fixtures paginate exactly once in UUID order, and `ROW_NUMBER` preserves global text ranks.
- Cursors are canonical versioned JSON authenticated by HMAC-SHA256 with an injected 256-bit local key. They bind the complete query/policy fingerprint, exact score bit pattern, timestamp, UUID, and cumulative result count. Tamper, signing-key mismatch, query drift, policy drift, invalid floats, and total-cap bypass fail closed without a result cache.
- Evidence comes only from matching non-suppressed durable AX/OCR/transcript spans or canonical title/application/URL fields; no generated summary is introduced. Cancellation is checked before archive access, after the bounded SQL page, and before return. Source frame publication now requires valid normalized window bounds so every result can satisfy the foreground evidence contract.
- Thirty-seven safe focused Release search/index/database/merge tests plus all eight parser package tests pass, as do contracts, privacy, strict formatting, universal compile-only Release, and binary dependency gates under the 50 ms encoder tripwire. No app or media runtime launched. Evidence: `Results/LM-037/report.json` and `Results/LM-037/story-gate.txt`.

## 2026-08-28 — LM-038 frozen lexical retrieval benchmark

- The deterministic private corpus now freezes 500 frames and 100 queries at SHA-256 `640abc4d73f66c9555cb54df277b0963eeae02cd80651f7174d6bba238e02912`. It contains the exact 30/20/25/15/10 category split, three assessor records per query, and byte-for-byte generator reproduction.
- The first generated freeze was invalid before engine tuning: a default ISO8601 parser rejected fractional-second timestamps and left ten site/time queries without the relevant IDs required by the plan. Its original hash, bytes, and failing baseline are preserved in LM-038 evidence. The correction reused the generator's fractional-second formatter and did not change any query, filter, frame, grade, or engine behavior.
- The 75-query lexical subset runs three times per query against 500 encrypted-archive frames. Recall@5 and Recall@10 are 1.0, MRR and nDCG@10 are 1.0, adversarial no-result precision is 1.0, and zero forbidden frames appear. Final p50/p95/p99 are 2.734/3.510/3.833 ms, far beneath the 300 ms p95 ceiling.
- Corpus reproduction, focused Release search tests, contracts, privacy smoke, strict formatting, universal compile-only Release, and binary dependency gates pass under the 50 ms encoder tripwire. No app, Apple ImageIO, HEVC, AVAssetWriter, or VideoToolbox runtime executed. Evidence: `Results/LM-038/retrieval.json` and `Results/LM-038/story-gate.txt`.

## 2026-08-28 — LM-039 shared search binding safe implementation; XCUITest blocked

- A single root-owned `SearchSessionModel` is injected into both the main Search section and global search panel. Its 150 ms debounce clears stale results immediately, cancels superseded work, rejects late pages by generation even when a provider ignores cancellation, and settles a generation exactly once.
- Production search requests derive their bundle/host allowlists only from frames whose visual state, parent media, and approved merged-text record are all ready. Suppressed projections never widen the request policy.
- Deterministic warm, slow, and content-free error XCUITest fixtures compile. Fourteen focused Release unit tests cover shared model behavior and the ready-only database scope; UI compile-only, contracts, privacy, formatting, dependency, and universal Release gates pass under the encoder tripwire.
- No application, XCUITest runtime, Apple ImageIO, AVAssetWriter, HEVC encoder, VideoToolbox, or encoder service ran. LM-039 is technically blocked only on executing the three compiled XCUITest fixtures on a known-safe validation Mac; the exact resume probe is in `Results/LM-039/blocker.md`.

## 2026-08-29 — LM-044 timeline interval query

- `ArchiveTimelineQuery` returns a canonical `TimelineSlice` for each calendar-day, six-hour, one-hour, or fifteen-minute page. Frame selection is half-open and admits only fully ready frame, media, and approved merged-text records; incomplete or suppressed projections cannot influence frames, transitions, or transcript markers.
- Application transitions use the last eligible approved bundle before the page as lookback and then follow deterministic frame time/UUID order. Gaps overlapping a page are clipped to the page boundary without changing their reason or approved identity, and excluded gaps remain identity-free.
- The pinned 24-hour fixture hash `ebdabc6f41d55df2a796b56814f16d78555af015904e1b34883c6ffdccb57d2a` yields four frames, four transitions, one transcript marker, and sleep/excluded/process-stopped gaps of exactly 1,200/600/1,800 elapsed seconds despite shuffled insertion order.
- Calendar pagination preserves the 23-hour spring-forward and 25-hour fall-back days in `America/Los_Angeles`; fixed zoom pages are half-open, so a boundary frame appears exactly once. Six focused Release tests, contracts, privacy, strict formatting, dependency audit, and universal compile pass under the encoder tripwire with no application or media runtime. Evidence: `Results/LM-044/report.json`.

## 2026-08-29 — LM-049 bundled MobileCLIP runtime

- The bundled MobileCLIP-S0 closure is pinned by an external manifest SHA-256, exact model version, 12-file artifact inventory, and 112,239,644-byte footprint. Verification rejects a rewritten manifest even when it contains updated artifact hashes, plus duplicate entries, unexpected files, missing files, invalid paths, byte drift, and every symlink before model load.
- Image preprocessing is a pure CoreVideo/Swift path: an already approved upright sRGB `ThumbnailRaster` receives a deterministic aspect-fill center crop, bilinear resize to 256×256, RGBA-to-BGRA conversion, and opaque alpha. Text preprocessing now throws on malformed resources instead of trapping and freezes the paired 77-token CLIP sequence; `a red square` begins `49406,320,736,3999,49407` and zero-pads the remainder.
- `MobileCLIPModelService` exposes `notChecked`, descriptor-bearing `ready`, and content-free typed `unavailable` states. Startup verifies the complete resource closure before loading either encoder. Integrity/model/tokenizer/inference failures remain fail-closed, do not retry implicitly, and cannot return embeddings; invalid image input does not poison an otherwise healthy model.
- Current bundled image and text inference reproduces the exact committed S3 parity vectors at cosine similarity ≥0.999999. The canonical S3 package metrics remain 1.0/0.99999988 image/text parity, 1.0 Recall@10, and 1.546/7.919 ms image/text p95.
- Eight focused Release unit tests and two bundled CoreML inference tests pass, as do contracts, privacy, strict formatting, dependency audit, and universal compile-only Release. The 50 ms encoder-service tripwire remained clear; no application, Apple ImageIO runtime, hardware encoder test, AVAssetWriter, or VideoToolbox path ran. Evidence: `Results/LM-049/report.json` and `Results/LM-049/story-gate.txt`.

## 2026-08-29 — LM-050 thermal- and power-aware visual embedding jobs

- `VisualEmbeddingJobProcessor` accepts only the current MobileCLIP image producer identity and an exact pending V2 frame whose parent media and thumbnail artifact are ready. The source provider verifies a regular non-symlink file and its SHA-256 before software HEIC decode, then revalidates capture epoch, foreground-window identity, policy generation, path, hash, and suppression state after inference and before publication.
- Output must be exactly 512 finite, nonzero values. Deterministic Double-accumulated L2 normalization produces a unit vector and canonical little-endian Float32 evidence; the frozen `[3,4,0…]` fixture hashes to `2cdd74c082e797454606fd2bd5ac4852cb42e0081ffc90c1890bbc25b24c8065`.
- Visual jobs inherit the durable single-job 120-second lease and bounded 5/10-second, three-attempt retry policy. Capture transitions never lease enrichment; serious thermal pressure, Low Power Mode, and battery at or below 20% defer visual work, while critical pressure pauses all enrichment. Permanent model failure is projected honestly instead of claiming searchable visual evidence.
- Producer-version synchronization atomically stales old visual artifacts and vector offsets, requeues eligible frames as pending, preserves suppressed frames, and is idempotent. The menu backlog separately exposes visual indexing and terminal-unavailable counts with bounded accessibility copy.
- Twenty focused Release tests pass across visual jobs, scheduler behavior, and the fail-closed MobileCLIP service. Canonical LM-007 evidence remains within capture gates at 11.118 ms p95, zero timer intervals over 100 ms, and one concurrent inference job. Contracts, privacy, strict formatting, dependency audit, and universal compile-only Release pass under the encoder tripwire; no application, Apple ImageIO runtime, hardware encoder test, AVAssetWriter, or VideoToolbox path ran. Evidence: `Results/LM-050/report.json` and `Results/LM-050/story-gate.txt`.

## 2026-08-29 — LM-051 append-safe model-versioned vector storage

- The production flat-file format is `DSVEC002`: a 192-byte immutable header binds the 512-dimensional/1,024-byte Float16 record geometry, exact model hash, producer and preprocessing hashes, generation UUID, and header SHA-256. The canonical path remains `vectors/mobileclip-s0/<model-hash>.f16`, and every file is owner-only mode `0600`.
- Publication is deliberately two-phase. A live visual lease and exact foreground source identity are revalidated, Float16 bytes are appended and fsynced, and the SQLite offset/artifact enter `staged`. Only after the scheduler durably commits job success does a second byte/hash/norm verification atomically promote the vector, artifact, and frame to `ready`; a staged or orphaned append is never searchable.
- Exact duplicate retries reuse the verified staged record without another append. A stale prior producer projection can be replaced by current leased work. Reads require the expected model/generation, aligned in-bounds offset, matching locator, per-vector SHA-256, finite decoded values, and the recorded near-unit Float16 norm.
- Recovery truncates an fsynced but unreferenced crash tail and promotes a staged record only when its exact job is already succeeded. Truncation, payload corruption, model mismatch, and duplicate offsets fail closed. Complete rebuild removes the incompatible projection, returns eligible frames to pending, resets visual jobs to queued, and recreates only a verified header.
- Compaction copies only verified ready/staged records into stable frame-ID order and uses a mode-`0600` recovery journal plus old/new generation identities. A crash after the file swap rolls back to the old file when SQLite is old; a crash after the SQLite update finishes cleanup around the new file. The 40-vector property fixture retains 26, removes 14 stale records, compacts to 26,816 bytes, and preserves every retained unit vector under shuffled reads.
- Seventeen focused Release tests cover the new store, LM-050 publication lifecycle, and existing exact-vector scanner. Canonical S4 remains 64.117 ms filtered p95 with 2.875 MB incremental RSS at one million vectors, stable ordering, and pre-result truncation detection. Contracts, privacy, strict formatting, dependency audit, and universal compile-only Release pass under the encoder tripwire with no app, ImageIO, or hardware-video runtime. Evidence: `Results/LM-051/report.json` and `Results/LM-051/story-gate.txt`.

## 2026-08-29 — LM-052 filtered exact visual-vector scan

- `ArchiveVectorStore.scanSnapshot` creates an immutable, model/generation-bound search view after compaction recovery and exact file/header validation. SQLite excludes non-ready offsets, vector artifacts, frames, and parent media before scoring and applies half-open time plus app/site filters there; suppression is therefore impossible to recover from vector bytes.
- `ExactVisualVectorSearcher` maps the active DSVEC002 file read-only, rejects size/header/model/generation and malformed-offset drift, packs sparse offsets into bounded chunks, coalesces contiguous runs, converts Float16 with vImage, and scores exact cosine products with vDSP. Cancellation is checked before mapping, at every chunk, and before projection. Top-k is bounded to 100 and stable by score, capture time, then lowercase UUID; every projected winner is hash-verified before any result returns.
- The production-scale gate ran 20 measured samples at each size: 100k p95 52.139 ms, 500k p95 232.038 ms, and 1M p95/p99 444.380/445.969 ms with 91.328 MB incremental resident memory. The separate canonical diverse S4 corpus remains within scalar error `1.371e-5`, stable ordering, truncation detection, and all latency/memory gates.
- Sixteen safe focused Release tests plus the explicit scale test pass, as do contracts, privacy, strict formatting, dependency audit, and universal compile-only Release. The 50 ms encoder-service tripwire remained clear; no application, Apple ImageIO runtime, hardware encoder test, AVAssetWriter, or VideoToolbox path ran. Evidence: `Results/LM-052/report.json`, `Results/LM-052/scale-metrics.json`, and `Results/LM-052/story-gate.txt`.

## 2026-08-29 — LM-053 visual-only shared search

- `LocalSearchEngine` now routes visual-only requests through a model-bound visual engine while preserving LM-037 lexical behavior for text and an explicit lexical fallback for hybrid until LM-054. The bundled MobileCLIP-S0 text encoder must match the exact vector model hash and dimension before the engine can be constructed.
- Time, application, policy-host, requested-host, ready-state, and suppression filters execute in SQLite before vector scoring. Policy expiry is checked around every async boundary, and projection revalidates the current frame/media/vector/artifact/model identity so a concurrent suppression cannot return stale evidence.
- Visual results carry only `.visual` evidence with no invented matched text. Canonical foreground/browser context is reconstructed from durable approved fields and fails closed if malformed. Archive media and thumbnail locators are returned only when `allowImageResources` is explicitly true; otherwise the engine returns an opaque, content-free resource identifier.
- The frozen 25-query visual subset runs end-to-end through the shared engine at Recall@10 1.0 with zero forbidden frames, above the 0.75 acceptance floor. Canonical real-model Recall@10 remains 1.0 and the paired text encoder reproduces its frozen vector at cosine similarity 0.99999988.
- Forty-two focused Release tests and two bundled CoreML parity tests pass, as do contracts, privacy, strict formatting, dependency audit, and universal compile-only Release. The 50 ms encoder tripwire remained clear; no application, Apple ImageIO runtime, hardware encoder test, AVAssetWriter, or VideoToolbox path ran. Evidence: `Results/LM-053/report.json`, `Results/LM-053/retrieval.json`, and `Results/LM-053/story-gate.txt`.

## 2026-08-29 — LM-054 deterministic hybrid fusion

- Hybrid search awaits bounded lexical and visual candidate pages concurrently, then applies reciprocal-rank fusion with fixed `k=60`; component scores are never compared directly. Exact frame ID, title, app/bundle, and host/full-URL matches receive frozen additive boosts of `0.025/0.020/0.015/0.010`, with no general recency boost.
- Duplicate frame IDs merge their verified source evidence and component ranks. Grouping requires the same application, normalized window, site, and at most 30 seconds; it then requires an exact media hash, a same-epoch `staticHeartbeat` that durably represents the capture-time 64×64 perceptual threshold, or approved-text token Jaccard of at least 0.8. The strongest capture remains the representative.
- Every page recomputes at most 100 candidates per component and applies grouping before the policy-wide cap. The versioned HMAC-SHA256 cursor binds normalized query, all filters and policy scope, exact score bits, timestamp, UUID, and cumulative count. It requires no result cache, rejects tamper/query/policy/key drift, and deliberately permits a smaller page size mid-chain. The `7/7/6` fixture returns exactly 20 unique frames with no skip.
- The production local search request is now hybrid. `SearchSessionModel` publishes no lexical interim page and settles once only after fusion, preventing repeated result jumps; model unavailability degrades to one final lexical page, while integrity failures remain fatal. Lexical, visual, and fused output all require explicit `allowImageResources` before exposing archive locators.
- Fifty-five focused Release tests and the frozen hand-computed fusion fixture pass, as do contracts, privacy, strict formatting, dependency audit, and universal compile-only Release. The 50 ms encoder tripwire remained clear; no application, Apple ImageIO runtime, hardware encoder test, AVAssetWriter, or VideoToolbox path ran. Evidence: `Results/LM-054/report.json`, `Results/LM-054/fusion.json`, and `Results/LM-054/story-gate.txt`.

## 2026-08-29 — LM-055 frozen hybrid retrieval and million-frame scale gate

- The exact LM-038 fixture remains byte-identical at SHA-256 `640abc4d73f66c9555cb54df277b0963eeae02cd80651f7174d6bba238e02912`; no relevance label changed after freeze. Its 500 frames and 100 queries retain three independent judgments per query, including all 25 visual-semantic cases.
- Actual LM-038 lexical result lists and the frozen visual consensus ordering feed the production LM-054 RRF ranker. Hybrid Recall@10 and nDCG@10 are both 1.0, versus lexical-only 0.75/0.75 and visual-only 0.35/0.35; visual-query Recall@10 is 1.0, duplicate rate is zero, and no forbidden result appears. The canonical real MobileCLIP proof remains Recall@10 1.0.
- The production exact Accelerate scanner and production concurrent hybrid engine ran twenty measured searches over one million 512-dimensional Float16 vectors. p95/p99 are 505.539/519.254 ms and incremental RSS is 50.781 MB, inside the fixed 750/1,000 ms and 500 MB gates. Exact search therefore remains the V1 design and no approximate-index ADR is warranted.
- Explicit recovery tests prove that truncation/checksum corruption returns no results before rebuild, vector compaction recovers both crash boundaries, and a missing/stale FTS projection rebuilds to exact ready-frame parity. Twenty focused safe search tests, the frozen retrieval test, three recovery tests, contracts, privacy, strict formatting, dependency audit, and universal compile-only Release passed under the 50 ms tripwire.
- No application, XCUITest, Apple ImageIO runtime, hardware encoder test, AVAssetWriter, or VideoToolbox path ran. No pending story now has all declared dependencies passed: LM-028's real eight-hour app/media soak and LM-039's required XCUITest fixtures remain technically blocked by the permanent runtime quarantine. Evidence: `Results/LM-055/retrieval.json`, `Results/LM-055/report.json`, and `Results/LM-055/story-gate.txt`.

## 2026-08-29 — LM-028 headless live-source safety probe quarantined

- A narrow command-line harness attempted to combine the real foreground-window ScreenCaptureKit source with the pinned software-only HEIC writer without launching the application. Static linkage had no direct ImageIO, VideoToolbox, MediaToolbox, or AVFoundation framework dependency, and permission-only preflight reported Screen Recording and Accessibility ready.
- The single five-second smoke exited with content-free `capture_or_decode_failed`, but `VTEncoderXPCService` remained with PID 70643, PPID 1, and 78 seconds elapsed. It was terminated immediately; no xcodebuild, xctest, harness, codec helper, or encoder-service process remained, and the live probe was not repeated.
- This disproves the assumption that the complete live ScreenCaptureKit source is hardware-safe on this Mac even when frame encoding is software-only. The experiment's uncommitted source/target was removed and its generated binary moved to Trash. ADR 0001 and the software HEIC representation remain accepted because no foreground isolation, local storage, identity, atomic publication, integrity, search evidence, retention, or forensic-deletion contract changed.
- LM-028 remains technically blocked on the real eight-hour production soak. LM-039 remains technically blocked on XCUITest runtime, so no pending story currently has all declared dependencies passed.

## 2026-08-29 — ADR 0006 isolated runtime-validation ledger

- The owner prohibited application, ScreenCaptureKit, ImageIO, VideoToolbox, and hardware-media runtime testing on this laptop. LM-028 and LM-039 remain technically blocked; neither acceptance criterion is waived or relabeled.
- ADR 0006 separates safe implementation dependency readiness from story completion. Before LM-064, a blocked story may feed downstream implementation only when all safe work is complete, `implementationReadiness` is `ready`, and H9 owns the exact missing runtime evidence. Unit/model/static/compile/snapshot proof is never described as runtime evidence.
- H9 runs once at LM-064 on a physically distinct isolated validation Mac and must execute every accumulated runtime ledger item in dependency order. Every deferred story must then receive its real evidence and become passed before LM-064 or any later release story can pass.

## 2026-08-29 — LM-040 visible search filters (runtime deferred)

- One root-owned `SearchFilterSessionModel` now backs both the main Search section and global panel. Parser text, explicit app/site pickers, approved autocomplete, calendar intervals, examples, token removal, and restored snapshots all publish the same canonical `SearchSessionInput` consumed by `SearchRequest`.
- App and host suggestions come only from fully ready, non-suppressed searchable archive records. Ambiguous application display names remain explicit picker options but are excluded from parser aliases, and an invalid parser context falls back to empty fail-closed behavior rather than trapping.
- Filter snapshots serialize stable arrays and visible tokens; restoration rejects an unapproved app/host, duplicate token identity, mismatched token/request state, or invalid interval without mutating the current search. Date controls use calendar arithmetic across DST boundaries.
- Fifteen `MemorySearch` tests, one focused `MemoryStore` metadata test, strict formatting, static audit, and a universal Release compile-only build pass under the 50 ms encoder tripwire. No app, XCUITest, ScreenCaptureKit runtime, Apple ImageIO runtime, VideoToolbox, or hardware-media path ran.
- LM-040 remains technically blocked because keyboard and VoiceOver interaction require application runtime. The exact unchanged acceptance ledger is assigned to H9; safe proof is recorded only as implementation evidence and is not represented as runtime evidence.

## 2026-08-29 — LM-041 adaptive result grid (runtime deferred)

- The shared main-search/global-panel result surface now uses an adaptive one/two/three-column `NSCollectionView` so collection reuse, rather than a SwiftUI `List`, bounds visible card construction. Opaque cursors are forwarded unchanged; new pages append in stable order, discard duplicate frame IDs, ignore late query generations, and preserve existing results on pagination failure.
- A root-owned actor cache is capped at 64 MiB and keys every decoded thumbnail by frame ID plus locator kind/value. Production lookup requires exact ready metadata, a regular non-symlink file, matching SHA-256, and pinned software-codec decode; per-frame generations reject stale completions.
- Cards retain capture time, app, approved host, evidence source/text, position, and stable identifiers. Empty archive/query, loading, no-result/removable-filter, indexing, page failure, and fatal failure are explicit, with deterministic accessibility announcement text.
- Nineteen safe MemorySearch tests pass. A pure model test projects 10,000 cards deterministically but makes no timing or rendering claim. Strict formatting, static audit, snapshot, and universal Release compile-only verification pass under the 50 ms tripwire; no app or prohibited media runtime ran.
- LM-041 remains technically blocked because the production 10,000-card S6, stale-thumbnail, selection, keyboard, and VoiceOver runtime evidence is unchanged and deferred to H9. The safe evidence is not represented as a substitute.

## 2026-08-29 — LM-042 exact source-evidence presentation (runtime deferred)

- Search cards now map every contract source to explicit copy: Accessibility text, On-screen text, Application, Window title, Page address, Transcript, or Visual similarity. The displayed snippet is copied only from validated `SearchEvidence.matchedText`; visual evidence never receives invented text.
- `summaryText` is structurally nil for every V1 card. Card titles remain canonical window-title/application fields, so a missing source snippet cannot silently become a generated claim.
- Text/visual component ranks and fused score are absent by default and projected only when the root search session receives the explicit `--search-diagnostics` flag. The overlay adds no captured content.
- Twenty-one safe MemorySearch tests, the seven-source snapshot, strict formatting, static/privacy/dependency audits, and universal Release compile-only verification pass under the 50 ms tripwire. No app or prohibited media runtime ran.
- LM-042 remains technically blocked behind LM-041 and the production source-indicator/diagnostics/accessibility inspection assigned to H9. Safe evidence is not represented as runtime evidence.

## 2026-08-29 — LM-043 exact-source detail canvas (runtime deferred)

- `ArchiveMomentSourceStore` admits only an exact schema-V2 frame in a ready chunk with a searchable, non-suppressed projection. Production admission then requires regular non-symlink media and manifest files, verifies their recorded hashes and sizes, decodes the canonical manifest, and binds the exact frame entry's epoch, target window, path, hash, and byte count before decode.
- A root-injected actor repository owns a 256 MiB decoded-raster LRU keyed by frame plus exact locator. It revalidates source integrity before every cached hit, discards late concurrent selection completions, and exposes typed source, integrity, manifest, corrupt-media, and invalid-raster failures. Retry addresses the same exact source.
- The detail composition has explicit loading/ready/recovery states, stable previous/next boundaries, aspect-fit through 8x zoom, pan disabled at fit and bounded to scaled overflow, and a direct CoreGraphics raster bridge cached by exact identity. Export revalidates and atomically writes the original HEIC without modifying archive state.
- Twenty-seven safe MemorySearch tests and two MemoryStore tests pass, including rapid-selection identity, cache eviction/revalidation, retry, transform bounds, sequence boundaries, exact ready-source admission, locator mismatch, and suppression. Strict formatting, static privacy/dependency audits, deterministic snapshot, and universal Release compile-only verification pass under the encoder tripwire.
- LM-043 remains technically blocked because real production software-HEIC cold/cached latency and application tamper/corrupt/manifest recovery, interaction, accessibility, and export evidence are prohibited on this laptop. ADR 0006 assigns the unchanged runtime ledger to H9; no safe evidence is represented as a substitute.

## 2026-08-29 — LM-045 exact timeline rail (runtime deferred)

- `MomentTimelineProjection` rejects any page whose timeline frames and exact search results differ by identity, time, foreground context, browser context, or thumbnail locator. It maps capture time into stable rail positions, keeps privacy gaps content-free, exposes four non-color-only gap patterns, and produces deterministic chronological moment/gap/transition accessibility rows.
- The feature-local timeline model publishes the nearest exact scrub target immediately, requests its low-resolution thumbnail, and promotes the exact full-frame result only after the specified 120 ms dwell. Generation checks plus task cancellation prevent an older thumbnail or dwell from replacing the latest target; keyboard stepping stops at page boundaries and zoom reloads around the exact settled frame.
- Production timeline composition uses the ready-only archive query and current exact source locator. The shared 64 MiB thumbnail cache now revalidates ready/suppression state, locator, regular non-symlink identity, and SHA-256 before every cached return and again after decode before insertion.
- Thirty-three safe MemorySearch tests and three MemoryStore tests pass. Strict formatting, static privacy/dependency audits, the deterministic timeline snapshot, and a universal arm64/x86_64 Release compile-only build pass under the 50 ms encoder tripwire. No application, software HEIC helper, ScreenCaptureKit, Apple ImageIO, VideoToolbox, or hardware-media runtime ran.
- LM-045 remains technically blocked because real S6 scrub latency, rapid-scrub pixels, keyboard/VoiceOver interaction, resize/theme/contrast/pseudo-localization review, and screenshot baselines are prohibited here. ADR 0006 assigns the unchanged ledger to H9; safe evidence is not represented as a substitute.

## 2026-08-29 — LM-046 Timeline section and safe Revisit (runtime deferred)

- Timeline day identity uses local Gregorian year/month/day components and calendar intervals rather than fixed 86,400-second arithmetic. Safe tests prove the 2026 Los Angeles spring-forward and fall-back days retain exact 23/25-hour elapsed durations and restore to the same local day under French and English locales.
- The Timeline section persists its selected date in navigation state, opens to today otherwise, provides previous/next/day-picker/today controls, renders locale-aware hour separators, exposes four zoom levels, shows patterned gaps, labels approved application transitions when zoomed in, summarizes them when zoomed out, and displays the exact selected result context.
- Revisit first revalidates the frame as current/ready, rebuilds the approved application/host scope from current searchable records, rejects an out-of-range frame, unapproved bundle/host, private context, or non-HTTP(S) scheme, and constructs only a bundle identifier plus origin/path URL. Form state is deliberately unrepresentable; NSWorkspace receives empty arguments and environment.
- Thirty-eight safe MemorySearch tests and three MemoryStore tests pass, with strict formatting, deterministic snapshot, privacy/dependency scans, and universal arm64/x86_64 Release compile-only verification under the 50 ms encoder tripwire. Neither the app nor NSWorkspace launch action nor any media runtime ran.
- LM-046 remains technically blocked because the real application navigation/restoration, locale/DST presentation, keyboard/VoiceOver, transition labeling, selected context, and NSWorkspace launch behavior are prohibited here. ADR 0006 assigns the unchanged ledger to H9; safe evidence is not represented as a substitute.

## 2026-08-29 — ADR 0007 faster personal-alpha verification

- Routine story checkpoints now run focused changed-behavior tests, changed-file strict formatting, a targeted static privacy/safety check, and one cached native arm64 compile only when integration requires it. Universal Release and broad cross-cutting suites move to Recall UI, Trust/Lifecycle/H9, Agent Access, Activity/Hardening, and final release checkpoints.
- The new cadence reduces each remaining broad gate from roughly 28 story-level executions to five milestone/final executions, an estimated 82% reduction, while preserving atomic story commits and every original acceptance criterion.
- LM-039–LM-046 remain technically blocked and implementation-ready for the single H9 runtime ledger; safe evidence is still not a substitute. The application, ScreenCaptureKit, ImageIO, VideoToolbox, and hardware-media quarantine on this laptop is unchanged.
- LM-075–LM-079 are explicitly `adr_removed`, not implemented or passed. LM-080 now depends on LM-074, giving the personal alpha a visual-memory critical path with no audio permission/model surface.
- Routine evidence retains concise command/result/hash/metric summaries and useful failure excerpts rather than verbose duplicate successful logs. Human observation windows remain fully credit-silent.

## 2026-08-29 — LM-047 provisional Forget Moment/Range (runtime deferred)

- One atomic store transaction resolves exact visible frame identities, writes the canonical `rewriting` tombstone, suppresses frame text/visual plus merged text, cancels incomplete derived jobs, enqueues a priority-1000 `mediaRewrite` job keyed by tombstone, and writes only actor/action/count in the audit row. The physical retained-frame rewrite remains LM-060 work.
- Moment and half-open Range tests prove lexical search, Timeline, and local approved-scope disappearance. A SearchSession read barrier retains suppressed frame IDs so even a late database snapshot or later pagination result cannot reintroduce them during processing.
- The encrypted file-backed restart test proves queued rewrite and suppression survive process loss. Permanent failure exposes only a bounded content-free error and never unhides the target.
- Detail and Timeline provide destructive Moment and Range/day actions. Shared confirmation defaults to Cancel, explains immediate hiding and backup limits, then shows queued/rewrite/verification/failure state. Production composition injects the store; fixtures cannot mutate an archive.
- Four focused MemoryStore tests, three Forget model tests, one late-result test, changed-file strict formatting, targeted static safety, deterministic snapshot, and cached native arm64 compile pass with no encoder-service tripwire event.
- LM-047 remains technically blocked for the real keyboard/VoiceOver/application/helper/relaunch/physical-rewrite flow assigned unchanged to H9. No safe proof is represented as runtime evidence.
